/* Public Supabase configuration.
   Replace the empty strings with your project's URL and anon/publishable key.
   Never put a Supabase service-role key here. */
window.ATTENDANCE_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
